node_modules
.next
out
dist
.DS_Store
*.log
*.tsbuildinfo
.env*.local
.vercel
.turbo
public/r/*.json
!public/r/.gitkeep
.superpowers/
.claude/

# pnpm 11 auto-regenerates this with invalid placeholder content; we pin
# pnpm 9 via packageManager and do not use a workspace file.
pnpm-workspace.yaml

# local planning/notes — not part of the shipped repo
/docs
